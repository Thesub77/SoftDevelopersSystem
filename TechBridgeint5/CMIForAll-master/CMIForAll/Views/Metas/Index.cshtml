@model IEnumerable<CMIForAll.Models.Meta>

@{
    ViewBag.Title = "Index";
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create</title>
    <link rel="stylesheet" href="~/Style/StyleforCMI.css">
</head>

<body>

    <hr>
    <hr>
    <hr>
    <hr>
    <hr>
    <h2>Metas creadas</h2>

    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor_esperado)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Fecha_limite)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Indicador.Nombre)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Descripcion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Valor_esperado)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Fecha_limite)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Indicador.Nombre)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }

    </table>
    <p>
        @Html.ActionLink("Crear meta", "Create")
    </p>
</body>
</html>
